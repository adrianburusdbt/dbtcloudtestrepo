models:
  - name: my_child_model
    config:
        group: slack
    
  - name: my_first_dbt_model 
    description: "A starter dbt model"
    latest_version: 2
    versions:
      - v: 1
      - v: 2
    
    
    config:
        group: slack
        contract:
          enforced: true
    columns:
        - name: id
          description: "The primary key for this table"
          tests:
            - unique
          data_type: int

  - name: my_first_dbt_model_v2
    description: "A starter dbt model"
    config:
        group: slack
        contract:
          enforced: true
    columns:
        - name: id
          description: "The primary key for this table"
          data_type: int

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
        - name: id
          description: "The primary key for this table"
          tests:
            - unique
    config: 
      group: slack
